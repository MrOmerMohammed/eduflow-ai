"use client";
import Link from "next/link";
import {useState} from "react";

export default function AiBoard({schoolId,school}:{schoolId:string;school:{name:string;code:string}}){
 const [message,setMessage]=useState(""),[loading,setLoading]=useState(false),[result,setResult]=useState<any>(null),[error,setError]=useState("");
 const ask=async()=>{if(!message.trim())return;setLoading(true);setError("");setResult(null);try{const r=await fetch("/api/ai",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({schoolId,message})});const b=await r.json();if(!r.ok)throw new Error(b.error||"AI request failed");setResult(b.data)}catch(e){setError(e instanceof Error?e.message:"AI request failed")}finally{setLoading(false)}};
 return <main className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">E</span><span>EduFlow AI</span></div><p className="sidebar-label">AI</p><nav className="side-nav"><Link href="/">Overview</Link><Link className="active" href={"/ai?schoolId="+schoolId}>AI Assistant</Link><Link href={"/students?schoolId="+schoolId}>Students</Link><Link href={"/analytics?schoolId="+schoolId}>Analytics</Link><Link href={"/attendance?schoolId="+schoolId}>Attendance</Link></nav><div className="sidebar-footer"><strong>{school.name}</strong><span>{school.code}</span></div></aside><section className="dashboard"><header className="topbar"><div><p className="eyebrow">V14 · AI Orchestration</p><h1>School AI Assistant</h1></div></header><section className="workspace-banner"><div><span className="muted">Controlled execution</span><h2>Understand → authorize → execute → audit</h2><p>AI actions use the same school permissions as the ERP. No unrestricted database mutation is exposed to the assistant.</p></div><div className="workspace-badge">Tool Registry</div></section><section className="workspace-banner"><div style={{width:"100%"}}><textarea aria-label="AI request" value={message} onChange={e=>setMessage(e.target.value)} placeholder="Try: Find admission number 1001, or show management dashboard" rows={5} style={{width:"100%",padding:"14px",borderRadius:"12px",border:"1px solid #ddd"}}/><button className="primary-link" disabled={!message.trim()||loading} onClick={()=>void ask()}>{loading?"Working…":"Ask EduFlow AI"}</button></div></section>{error&&<p className="error page-message">{error}</p>}{result&&<section className="workspace-banner"><div><span className="muted">Authorized tool</span><h2>{result.tool}</h2><p>Status: {result.status}</p><pre style={{whiteSpace:"pre-wrap",marginTop:"16px"}}>{JSON.stringify(result.result,null,2)}</pre></div></section>}</section></main>;
}