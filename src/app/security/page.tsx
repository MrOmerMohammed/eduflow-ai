import Link from "next/link";
export default function SecurityPage(){return <main className="legal-shell"><Link href="/" className="auth-link">← EduFlow AI</Link><h1>Security</h1><p>EduFlow AI uses authenticated sessions, tenant-aware school memberships, role and permission checks, PostgreSQL row-level security where applicable, controlled server-side actions and audit logging.</p><h2>Tenant isolation</h2><p>School and organization identifiers are carried through authorized server-side workflows. Database functions used for privileged operations validate the acting user and school context.</p><h2>AI safety</h2><p>The AI layer uses a tool registry and authorization checks. Model-generated plans are not intended to bypass database authorization.</p><h2>Operational security</h2><p>Secrets are kept outside source control. Production deployments should use least-privilege credentials, regular dependency updates, backups, monitoring and a documented incident response process.</p><p className="success">Security controls are actively being hardened before general availability.</p></main>}
