"use client";
import Link from "next/link";
import { useState } from "react";

type Props={schoolId:string;school:{name:string;code:string};years:{id:string;name:string;is_current:boolean}[];grades:{id:string;name:string;code:string}[]};
export default function FinanceBoard({schoolId,school,years,grades}:Props){
 const [yearId,setYearId]=useState(years.find(y=>y.is_current)?.id??years[0]?.id??"");
 const [name,setName]=useState(""); const [feeType,setFeeType]=useState("tuition"); const [frequency,setFrequency]=useState("annual"); const [amount,setAmount]=useState("25000"); const [dueDay,setDueDay]=useState("10"); const [gradeId,setGradeId]=useState("");
 const [structureId,setStructureId]=useState<string|null>(null); const [message,setMessage]=useState(""); const [error,setError]=useState("");
 const post=async(action:string,payload:Record<string,unknown>)=>{const r=await fetch("/api/gateway",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,payload})});const b=await r.json();if(!r.ok)throw new Error(b.error||"Request failed");return b};
 async function createStructure(){setError("");setMessage("");try{const b=await post("finance.structure.create",{schoolId,academicYearId:yearId,name,feeType,frequency,amount:Number(amount),dueDay:dueDay?Number(dueDay):null,gradeId:gradeId||null});setStructureId(b.data);setMessage("Fee structure created successfully.")}catch(e){setError(e instanceof Error?e.message:"Unable to create fee structure")}}
 return <main className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">E</span><span>EduFlow AI</span></div><p className="sidebar-label">Workspace</p><nav className="side-nav">
<Link href={`/?schoolId=${schoolId}`}>Overview</Link>
<Link href={`/analytics?schoolId=${schoolId}`}>Analytics</Link>
<Link className="" href={`/students?schoolId=${schoolId}`}>Students</Link>
<Link href={`/academic?schoolId=${schoolId}`}>Academic</Link>
<Link className="" href={`/attendance?schoolId=${schoolId}`}>Attendance</Link>
<Link className="" href={`/exams?schoolId=${schoolId}`}>Exams</Link>
<Link className="active" href={`/finance?schoolId=${schoolId}`}>Finance</Link>
<Link className="" href={`/hr?schoolId=${schoolId}`}>Staff & HR</Link>
<Link href={`/communication?schoolId=${schoolId}`}>Communication</Link>
<Link href={`/notifications?schoolId=${schoolId}`}>Notifications</Link>
<Link href={`/parent?schoolId=${schoolId}`}>Parent Portal</Link>
<Link href={`/ai?schoolId=${schoolId}`}>AI Assistant</Link>
<Link href="/school/setup">School setup</Link>
</nav><div className="sidebar-footer"><strong>{school.name}</strong><span>{school.code}</span></div></aside><section className="dashboard"><header className="topbar"><div><p className="eyebrow">V7 · Fee Management & Finance</p><h1>Finance workspace</h1></div></header><section className="workspace-banner"><div><span className="muted">Collections workspace</span><h2>Configure fees and prepare collections workflows</h2><p>Configure fee structures first; assignment, invoice and payment actions are secured in the gateway and ready for the next finance UI layer.</p></div><div className="workspace-badge">RBAC + RLS + audit</div></section><section className="student-table-card"><h2>Create fee structure</h2><div className="student-toolbar"><select aria-label="Academic year" value={yearId} onChange={e=>setYearId(e.target.value)}>{years.map(y=><option key={y.id} value={y.id}>{y.name}{y.is_current?" · Current":""}</option>)}</select><input aria-label="Fee name" placeholder="Fee name" value={name} onChange={e=>setName(e.target.value)}/><select value={feeType} onChange={e=>setFeeType(e.target.value)}><option value="tuition">Tuition</option><option value="admission">Admission</option><option value="transport">Transport</option><option value="hostel">Hostel</option><option value="library">Library</option><option value="exam">Exam</option><option value="activity">Activity</option><option value="other">Other</option></select><select value={frequency} onChange={e=>setFrequency(e.target.value)}><option value="one_time">One time</option><option value="monthly">Monthly</option><option value="quarterly">Quarterly</option><option value="term">Term</option><option value="annual">Annual</option></select><input type="number" min="1" aria-label="Amount" value={amount} onChange={e=>setAmount(e.target.value)}/><input type="number" min="1" max="28" aria-label="Due day" value={dueDay} onChange={e=>setDueDay(e.target.value)}/><select aria-label="Grade" value={gradeId} onChange={e=>setGradeId(e.target.value)}><option value="">All grades</option>{grades.map(g=><option key={g.id} value={g.id}>{g.name}</option>)}</select><button className="primary-link" onClick={createStructure} disabled={!name||!yearId||Number(amount)<=0}>Create structure</button></div>{structureId&&<p className="muted">Structure created: {structureId.slice(0,8)}…</p>}</section><section className="metric-grid"><article className="metric"><span>Fee structures</span><strong>Ready</strong><small>Configured through gateway</small></article><article className="metric"><span>Invoices</span><strong>Gateway ready</strong><small>Invoice action secured</small></article><article className="metric"><span>Payments</span><strong>Gateway ready</strong><small>Payment action secured</small></article><article className="metric"><span>Security</span><strong>RLS</strong><small>Finance permission gated</small></article></section>{error&&<p className="error page-message">{error}</p>}{message&&<p className="success page-message">{message}</p>}</section></main>;
}
